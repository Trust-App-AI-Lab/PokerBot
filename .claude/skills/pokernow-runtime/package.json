{
  "name": "pokernow-runtime",
  "version": "1.0.0",
  "description": "Self-contained Poker Now bot — JS bridge + Python brain, Claude as decision engine",
  "private": true,
  "scripts": {
    "bridge": "node scripts/bridge.js",
    "check": "python3 scripts/decide.py --check",
    "state": "python3 scripts/decide.py --state",
    "auto": "python3 scripts/decide.py --auto"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "ws": "^8.16.0"
  }
}
